% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_xpath_reports.R
\name{generate_xpath_report}
\alias{generate_xpath_report}
\title{Generate an XPATH Summary Report from a DuckDB Database}
\usage{
generate_xpath_report(
  year,
  base_path,
  output_dir = "xpath_reports",
  shutdown = TRUE
)
}
\arguments{
\item{year}{Integer or character. Tax year of the database (used to locate the file
and label the output CSV).}

\item{base_path}{Character. Base directory containing year-specific subfolders
with the DuckDB database files (e.g., \code{EFILE2024.duckdb}).}

\item{output_dir}{Character. Directory where the CSV report will be written.
Defaults to \code{"xpath_reports"}.}

\item{shutdown}{Logical. Whether to shut down DuckDB completely after disconnecting.
Defaults to \code{TRUE}.}
}
\value{
Invisibly returns a data frame with columns:
\describe{
\item{xpath}{Unique XPATH2 string}
\item{count_occurrences}{Number of occurrences in \code{FLATXML}}
\item{schema_versions}{Comma-separated list of distinct schema versions}
}
}
\description{
Connects to a DuckDB database containing IRS e-file XML tables and produces
a summary report of XPATH usage across filings. The report includes:
\itemize{
\item One row per unique \code{XPATH2} value from the \code{FLATXML} table
\item A count of occurrences across all filings
\item A concatenated list of unique XML schema versions from the \code{KEYS} table
}
}
\details{
The function performs an inner join between \code{FLATXML} and \code{KEYS} on
\code{OBJECTID} (after removing duplicate \verb{(OBJECTID, VERSION)} pairs to avoid
Cartesian product inflation). It then aggregates results by \code{XPATH2}.

The result is written to a CSV file in the specified \code{output_dir}
and returned (invisibly) as a data frame.
}
\examples{
\dontrun{
generate_xpath_report(
  year = 2024,
  base_path = "C:/Users/jdlec/DATA/DUCKDB_2025"
)
}

}
