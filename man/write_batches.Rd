% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_create_batchfiles.R
\name{write_batches}
\alias{write_batches}
\title{Write batch files to disk}
\usage{
write_batches(batchfile, path = ".")
}
\arguments{
\item{batchfile}{Named list where each element contains a vector
(e.g., URLs or filenames) to be processed in one batch.}

\item{path}{Directory in which to create the \verb{batches/} subfolder.
Defaults to the current working directory.}
}
\value{
Invisibly returns \code{NULL}. Files are created as a side effect.
}
\description{
Persists each element of a batch list as a standalone \code{.R} file
containing an object named \code{x}. This allows parallel workers to
safely read and process batches without contention for a shared
\code{.RDS} file.
}
\details{
Each batch is written to a file named \verb{batches/<batchname>.R} inside
the specified path. The function uses \code{purrr::iwalk()} to iterate over
the list, ensuring the batch name and data stay paired correctly.
}
\examples{
\dontrun{
batches <- list(G01 = c("file1.xml", "file2.xml"),
                G02 = c("file3.xml", "file4.xml"))
write_batches(batches, path = "data/2021")
}
}
