% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_build_database.R
\name{validate_merge}
\alias{validate_merge}
\title{Validate a merged DuckDB database against its worker sources}
\usage{
validate_merge(main_db, worker_dbs)
}
\arguments{
\item{main_db}{Path to the main DuckDB database (e.g. \code{"EFILE2021.duckdb"}).}

\item{worker_dbs}{Character vector of worker database file paths
(e.g. all \code{worker_XX_YYYY.duckdb} files).}
}
\value{
A tibble summarizing row counts for each table:
\itemize{
\item \code{table_name} – table name
\item \code{main_rows} – number of rows in the merged database
\item \code{worker_sum} – total rows across all workers
\item \code{n_workers} – number of workers containing that table
\item \code{match} – TRUE/FALSE indicating if totals match
}
}
\description{
Compares table structures and row counts between a main merged DuckDB
and the worker databases it was built from.  Each table's row counts
from all workers are summed and compared with the main database total.
}
\examples{
\dontrun{
validate_merge("EFILE2021.duckdb",
               list.files("2021", pattern = "worker_.*\\\\.duckdb$", full.names = TRUE))
}
}
