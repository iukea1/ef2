% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_extract_csv_tables.R
\name{extract_csv_tables}
\alias{extract_csv_tables}
\title{Extract All IRS 990 Tables from DuckDB Databases}
\usage{
extract_csv_tables(
  wd,
  years,
  table_names = NULL,
  ccf = NULL,
  table_headers = NULL
)
}
\arguments{
\item{wd}{Character. Base working directory containing yearly subfolders with
DuckDB databases.}

\item{years}{Integer vector. Tax years to process (e.g., 2009:2024).}

\item{table_names}{Character vector of IRS 990 table names (defaults to
\code{irs990efile::get_table_names()} if not supplied).}

\item{ccf}{Data frame. Concordance crosswalk used for variable alignment.}

\item{table_headers}{Data frame. Output of \code{get_table_headers()}, providing
schema details for relational table construction.}
}
\value{
Invisibly returns \code{NULL}. Side effects: writes CSV tables for each year.
}
\description{
Iterates over a set of DuckDB database files, one per tax year, and extracts
all tables defined in the IRS 990 efile schema. The function builds both
header tables T00 and data tables T01 to T99 for each year, saving
them into the database as flat relational tables.
}
\details{
The working directory (\code{wd}) should contain subdirectories named for each tax year,
and each of those subdirectories must include a DuckDB file such as
"EFILE2024.duckdb". The function calls helper functions like
\code{build_table()} and \code{build_rdb_table()} to populate these databases.
}
\examples{
\dontrun{
extract_csv_tables(
  wd = "C:/Users/jdlec/DATA/DUCKDB_2025",
  years = 2009:2024
)
}

}
