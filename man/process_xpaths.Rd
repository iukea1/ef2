% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_xpath_reports.R
\name{process_xpaths}
\alias{process_xpaths}
\title{Combine and Process Multi-Year XPATH Reports}
\usage{
process_xpaths(years, base_path, concordance = NULL, get_type = NULL)
}
\arguments{
\item{years}{Numeric vector of tax years to process.}

\item{base_path}{Character. Base path containing yearly DuckDB folders and
the \code{xpath_reports} directory.}

\item{concordance}{Optional data frame containing a variable \code{xpath} for merging.
If not supplied, the merge step is skipped.}

\item{get_type}{Optional function that accepts an XPATH string and returns a type label.
Used to classify paths into \code{"HEADER"} or \code{"DATA"}.}
}
\value{
Invisibly returns a data frame containing all combined and processed XPATHs.
}
\description{
The \code{process_xpaths()} function automates the full workflow of generating,
combining, and summarizing XPATH usage reports across multiple DuckDB databases.
It can process multiple tax years, merge the resulting reports, summarize
schema versions, and add derived metadata such as first/last year and
current version indicators.
}
\details{
This function:
\enumerate{
\item Iterates over a range of tax years and calls \code{generate_xpath_report()} for each.
\item Reads all CSV reports from the specified \code{xpath_reports} directory.
\item Combines and aggregates XPATH occurrences across years.
\item Cleans and consolidates schema version lists using \code{combine_lists()}.
\item Extracts first and last schema years and merges results with a
\code{concordance} table if provided.
}

The final combined report is saved to \code{"xpath_reports/ALL-XPATHS.csv"}.
}
\examples{
\dontrun{
process_xpaths(
  years = 2009:2024,
  base_path = "C:/Users/jdlec/DATA/DUCKDB_2025",
  concordance = concordance,
  get_type = get_type
)
}

}
