% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_create_batchfiles.R
\name{gather_batches}
\alias{gather_batches}
\title{Load pending batch files from disk}
\usage{
gather_batches(path = ".")
}
\arguments{
\item{path}{Directory containing a \verb{batches/} subfolder.
Defaults to the current working directory.}
}
\value{
Named list of batch contents.
}
\description{
Reads all \code{.R} files from a \verb{batches/} directory and reconstructs
them as a named list, where each element contains the object \code{x}
defined inside the file.
}
\details{
Each batch file is read in isolation using \code{sys.source()} into a
temporary environment to avoid polluting the global environment.
The result is a list of batches, with names derived from the
filenames (minus the \code{.R} extension).
}
\examples{
\dontrun{
batches <- gather_batches("data/2021")
names(batches)
}
}
